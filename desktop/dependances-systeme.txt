# Bibliothèques système que Tauri et ses dépendances réclament pour compiler
# l'application sous Linux (relevé sur Ubuntu 24.04). Une seule liste, lue par
# le flux « Contrôles » et citée par le README : écrite à deux endroits, l'un
# des deux finit faux — c'est arrivé le 23/09/2026, le flux en portait cinq sur
# sept et s'arrêtait sur `alsa-sys`.
# Une par ligne, les lignes vides et les commentaires sont ignorés.

# WebView : c'est elle qui affiche l'interface.
libwebkit2gtk-4.1-dev
libjavascriptcoregtk-4.1-dev

# Fenêtre, icône de barre d'état, images SVG, HTTP de la WebView.
libgtk-3-dev
libayatana-appindicator3-dev
librsvg2-dev
libsoup-3.0-dev

# Son : `cpal` (par `alsa-sys`) l'exige même pour compiler les bancs, parce que
# l'écoute du microphone est dans le même binaire. Oubliée, la compilation
# s'arrête sur « Package 'alsa', required by 'virtual:world', not found ».
libasound2-dev
